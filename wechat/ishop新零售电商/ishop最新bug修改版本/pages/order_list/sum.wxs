var calculate = {
  sum: function (array) {
    var total = 0
    for (var i = 0; i < array.length; i++) {
      total += array[i].number
    }
    return total
  }
}

module.exports = {
  sum: calculate.sum
}